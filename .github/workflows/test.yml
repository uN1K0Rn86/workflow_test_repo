name: Testing stuff

on:
  push:
    branches:
      - main

env:
  COMMIT_MESSAGES: ${{ toJson(github.event.commits.*.message) }}
  CONDITION: ${{ contains(join(github.env.COMMIT_MESSAGES), '#skip') }}

jobs:
  a_test_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - if: ${{ github.event_name == 'push' && env.CONDITION == 'false' }}
        run: echo 'version will be tagged'
